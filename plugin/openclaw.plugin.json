{
  "name": "arise-browser",
  "version": "0.2.0",
  "description": "AI browser automation — persistent refs, multi-strategy clicks, behavior recording, virtual display with WebRTC streaming for headed mode on servers",
  "tools": [
    {
      "name": "arise-browser",
      "description": "Control a Chromium browser for web automation. Actions: navigate, snapshot, click, type, scroll, hover, select, focus, press_key, screenshot, text, tabs, recording.",
      "inputSchema": {
        "type": "object",
        "properties": {
          "action": {
            "type": "string",
            "description": "The action to perform",
            "enum": [
              "navigate",
              "snapshot",
              "click",
              "type",
              "scroll",
              "hover",
              "select",
              "focus",
              "press_key",
              "screenshot",
              "text",
              "tabs",
              "tab_create",
              "tab_close",
              "tab_switch",
              "evaluate",
              "recording_start",
              "recording_stop",
              "recording_export"
            ]
          },
          "url": {
            "type": "string",
            "description": "URL for navigate action"
          },
          "ref": {
            "type": "string",
            "description": "Element ref for click/type/hover/select/focus actions"
          },
          "text": {
            "type": "string",
            "description": "Text to type"
          },
          "value": {
            "type": "string",
            "description": "Value for select action"
          },
          "keys": {
            "type": "array",
            "items": { "type": "string" },
            "description": "Keys for press_key action"
          },
          "direction": {
            "type": "string",
            "enum": ["up", "down"],
            "description": "Scroll direction"
          },
          "amount": {
            "type": "number",
            "description": "Scroll amount in pixels"
          },
          "format": {
            "type": "string",
            "enum": ["yaml", "json", "compact", "text"],
            "description": "Snapshot format"
          },
          "diff": {
            "type": "boolean",
            "description": "Return diff only for snapshot"
          },
          "tabId": {
            "type": "string",
            "description": "Tab ID for tab operations"
          },
          "expression": {
            "type": "string",
            "description": "JavaScript expression for evaluate"
          },
          "recordingId": {
            "type": "string",
            "description": "Recording ID for recording operations"
          },
          "task": {
            "type": "string",
            "description": "Task description for recording export"
          }
        },
        "required": ["action"]
      }
    }
  ],
  "configSchema": {
    "type": "object",
    "properties": {
      "port": {
        "type": "number",
        "description": "Server port",
        "default": 9867
      },
      "host": {
        "type": "string",
        "description": "Bind address",
        "default": "127.0.0.1"
      },
      "token": {
        "type": "string",
        "description": "Bearer auth token"
      },
      "headless": {
        "type": "boolean",
        "description": "Run headless",
        "default": true
      },
      "profile": {
        "type": "string",
        "description": "Browser profile directory"
      },
      "virtualDisplay": {
        "type": "boolean",
        "description": "Enable Docker Neko virtual display (Linux). Runs a real headed Chrome inside a Docker container with WebRTC streaming so users can watch and interact via browser.",
        "default": false
      },
      "nekoPort": {
        "type": "number",
        "description": "Neko WebRTC UI port (virtual display mode)",
        "default": 6090
      },
      "nekoPassword": {
        "type": "string",
        "description": "Neko viewer password (virtual display mode)"
      },
      "nekoAdminPassword": {
        "type": "string",
        "description": "Neko admin password (virtual display mode)"
      }
    }
  },
  "lifecycle": {
    "start": "npx arise-browser --port {{port}} --host {{host}}{{#if virtualDisplay}} --virtual-display --neko-port {{nekoPort}} --neko-password {{nekoPassword}} --neko-admin-password {{nekoAdminPassword}}{{/if}}",
    "health": "http://{{host}}:{{port}}/health",
    "stop": "SIGTERM"
  }
}
